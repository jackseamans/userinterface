let img;
let p;

function setup() {
    noCanvas();
// p = createP('Your Bank of America credit (2202) has been charged. The order will be ready in 5 minutes, extending your route time to 10 minutes.');
// p.style('font-size', '24px');
// p.position(100, 40);
img = createImg('foodGif.gif');
  img.size(450, 450);
}